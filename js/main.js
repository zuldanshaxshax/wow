let course = document.querySelectorAll('#course');

let showCourse = document.querySelector('.show-course');


course.forEach((showC) => {
    showC.addEventListener('click', () => {
       alert('welcome');
    })
})